# Keep the live runtime small. PyArrow is pinned below 25 because the 25.0.0
# Community Cloud runtime can crash during startup. Optional reference-model
# packages live in requirements-dev.txt; the deployed app does not need them.
streamlit==1.59.2
pyarrow==24.0.0
