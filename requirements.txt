# The live app deliberately needs only Streamlit. Its pandas, NumPy, and
# PyArrow dependencies are installed with Streamlit itself. Optional packages
# used for development and the reference model live in requirements-dev.txt.
streamlit==1.59.2
